{
  "name": "garry",
  "version": "0.1.0",
  "description": "Run gstack (garrytan/gstack) in full isolation from your local Claude Code config",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/meatcar/garry.git"
  },
  "keywords": ["claude", "claude-code", "gstack", "sandbox", "isolation"],
  "bin": {
    "garry": "./src/cli.ts"
  },
  "type": "module",
  "scripts": {
    "check": "tsc --noEmit",
    "test": "bun test",
    "update-pin": "bun run scripts/update-nixpkgs-pin.ts"
  },
  "engines": {
    "bun": ">=1.0.0"
  },
  "devDependencies": {
    "@tsconfig/bun": "^1.0.10",
    "bun-types": "^1.3.14",
    "typescript": "^7.0.2"
  }
}

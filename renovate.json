{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["config:recommended"],
  "nix": {
    "enabled": false,
    "description": "flake.lock updates would silently unpin nixpkgs-playwright; the pin is managed by the update-pin workflow instead"
  },
  "packageRules": [
    {
      "description": "devDependency bumps only touch the type layer; CI (typecheck + tests) gates the merge",
      "matchDepTypes": ["devDependencies"],
      "automerge": true
    },
    {
      "description": "Action bumps are validated by CI running on them",
      "matchManagers": ["github-actions"],
      "automerge": true
    }
  ]
}
